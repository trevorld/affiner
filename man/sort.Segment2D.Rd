% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment.R
\name{sort.Segment2D}
\alias{sort.Segment2D}
\title{Sort segments by painter's depth}
\usage{
\method{sort}{Segment2D}(x, decreasing = FALSE, ..., scale = 1, alpha = angle(45, "degrees"))
}
\arguments{
\item{x}{A \link{Segment2D} object.}

\item{decreasing}{If \code{TRUE} sort closest first instead of farthest first.}

\item{...}{Passed to \code{\link[=painter_depth.Segment2D]{painter_depth.Segment2D()}}.}

\item{scale, alpha}{Oblique projection parameters passed to \code{\link[=painter_depth.Segment2D]{painter_depth.Segment2D()}}.
Defaults to \code{scale = 1} rather than \code{0} because \code{scale = 0} yields zero depth
for all \code{Coord2D} points (z is always 0), making sorting impossible.
All positive \code{scale} values produce the same ordering, so \code{1} is used to
avoid floating-point attenuation from small values.}
}
\value{
A \link{Segment2D} object sorted by painter's depth.
}
\description{
\code{sort.Segment2D()} sorts a \link{Segment2D} vector by painter's depth at each
segment's midpoint (see \code{\link[=painter_depth]{painter_depth()}}).
The default order (farthest first) is the draw order required by the
painter's algorithm.
}
\examples{
p1 <- as_coord2d(x = c(0, 2, 1), y = c(0, 0, 2))
p2 <- as_coord2d(x = c(2, 1, 0), y = c(0, 2, 0))
s <- as_segment2d(p1, p2 = p2)
sort(s, alpha = degrees(45))
}
