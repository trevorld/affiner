% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coord-methods.R, R/line_plane-methods.R
\name{bounding_ranges}
\alias{bounding_ranges}
\alias{range.Coord1D}
\alias{range.Coord2D}
\alias{range.Coord3D}
\alias{range.Segment2D}
\alias{range.Ellipse2D}
\alias{range.Point1D}
\alias{range.Line2D}
\alias{range.Plane3D}
\title{Compute axis-aligned ranges}
\usage{
\method{range}{Coord1D}(..., na.rm = FALSE)

\method{range}{Coord2D}(..., na.rm = FALSE)

\method{range}{Coord3D}(..., na.rm = FALSE)

\method{range}{Segment2D}(..., na.rm = FALSE)

\method{range}{Ellipse2D}(..., na.rm = FALSE)

\method{range}{Point1D}(..., na.rm = FALSE)

\method{range}{Line2D}(..., na.rm = FALSE)

\method{range}{Plane3D}(..., na.rm = FALSE)
}
\arguments{
\item{...}{\link{Coord1D}, \link{Coord2D}, \link{Coord3D}, \link{Ellipse2D}, \link{Line2D},
\link{Plane3D}, \link{Point1D}, or \link{Segment2D} object(s)}

\item{na.rm}{logical, indicating if \code{NA}'s should be omitted}
}
\value{
Either a \link{Coord1D}, \link{Coord2D}, or \link{Coord3D} object of length two.
The first element will have the minimum x/y(/z) coordinates
and the second element will have the maximum x/y(/z) coordinates
of the axis-aligned ranges.
}
\description{
\code{range()} computes axis-aligned ranges for \link{Coord1D}, \link{Coord2D},
\link{Coord3D}, \link{Ellipse2D}, \link{Line2D}, \link{Plane3D}, \link{Point1D}, and \link{Segment2D}
class objects.
Note that for \link{Line2D} and \link{Plane3D} objects the range may be \code{-Inf} to
\code{Inf} in one or more dimensions since lines and planes have infinite extent.
}
\examples{
range(as_coord2d(rnorm(5), rnorm(5)))
range(as_coord3d(rnorm(5), rnorm(5), rnorm(5)))

e <- as_ellipse2d(as_coord2d(c(0, 1), c(0, 1)), rx = c(1, 2), ry = c(0.5, 1))
range(e)

# A vertical line (x = 2) and a horizontal line (y = 3):
# x range covers only {2}, but y is Inf for the vertical line
l <- as_line2d(a = c(1, 0), b = c(0, 1), c = c(-2, -3))
range(l)

# A plane perpendicular to the x-axis at x = 5
range(as_plane3d(a = 1, b = 0, c = 0, d = -5))

range(as_point1d(a = c(1, 1), b = c(-2, -5)))

s <- as_segment2d(as_coord2d(c(0, 1), c(0, 0)),
                  p2 = as_coord2d(c(1, 1), c(0, 1)))
range(s)
}
